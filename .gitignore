node_modules/
dist/
.env
.env.bat
*.local
app.md
.claude/

# SSH keys downloaded from cloud providers (Oracle, etc.) — never commit these
*.key
*.key.pub
*.pem

# Stray output file from local shell experiments
/icacls

# Personal SSH launcher — hardcodes this machine's VM IP/key, not portable
/ssh-vm.bat