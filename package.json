{
  "name": "mwapnet-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for Tauri tooling — the desktop shell for MWAPNET. src-tauri/ is a sibling of client/ and server/; frontendDist points at ../client/dist.",
  "scripts": {
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
