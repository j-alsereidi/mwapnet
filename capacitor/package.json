{
  "name": "mwapnet-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "Android shell for MWAPNET — wraps client/dist via Capacitor.",
  "scripts": {
    "sync": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/preferences": "^8.0.1",
    "@capawesome-team/capacitor-android-foreground-service": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  }
}
